# Python API reference

This reference supplements the [browser tool documentation](tools-reference/lifecycle.md)
with implementation docstrings for contributors. The tool pages remain the
client-facing reference for parameters, examples and error responses.

## Configuration

::: justpen_browser_mcp.config

## Instance lifecycle

::: justpen_browser_mcp.instance_manager

## Errors

::: justpen_browser_mcp.errors

## Response helpers

::: justpen_browser_mcp.responses

## Operation metadata and evidence

::: justpen_browser_mcp.operations

::: justpen_browser_mcp.events

Update docstrings alongside API changes. The pre-push hook runs a strict
documentation build to validate this reference and local links; use
`make docs-build` for earlier feedback when needed.
